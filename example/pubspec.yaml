name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  auth_management:
    path: ../
  auth_management_apple_delegate:
    path: ../auth_management_apple_delegate
  auth_management_biometric_delegate:
    path: ../auth_management_biometric_delegate
  auth_management_facebook_delegate:
    path: ../auth_management_facebook_delegate
  auth_management_google_delegate:
    path: ../auth_management_google_delegate

  flutter_andomie: ^0.5.17
  flutter_androssy: ^0.3.5
  firebase_core: ^3.2.0
  google_sign_in: ^6.2.1
  firebase_auth: ^5.1.2
  shared_preferences: ^2.3.2
  flutter_entity: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true