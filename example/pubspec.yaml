name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  auth_management:
    path: ../
  flutter_andomie: ^0.5.18
  flutter_androssy: ^0.1.9
  firebase_core: ^3.1.1
  auth_management_apple_delegate: ^1.0.3
  auth_management_biometric_delegate: ^1.0.3
  auth_management_facebook_delegate: ^1.0.3
  auth_management_google_delegate: ^1.0.4

  pinput: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/