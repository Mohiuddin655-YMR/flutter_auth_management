name: auth_management
description: Collection of auth service with advanced controlling system.
version: 0.3.1
repository: https://github.com/P-YMR/flutter_auth_management.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_entity: ^1.0.3
  connectivity_plus: ^6.0.3
  firebase_auth: ^5.1.1
  auth_management_delegates: ^1.0.2
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter: