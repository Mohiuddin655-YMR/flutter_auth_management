name: auth_management
description: Collection of auth service with advanced controlling system.
version: 0.1.0-alpha3
homepage: https://github.com/P-YMR/flutter_auth_management.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_andomie: ^0.4.9-alpha1

  # Authentications
  firebase_auth: ^4.6.3
#  flutter_facebook_auth: ^5.0.7
  google_sign_in: ^6.1.4
  local_auth: ^2.1.6
  sign_in_with_apple: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: