name: auth_management_firebase_delegate
description: "Auth management delegate simplifies login with support for OAuth and biometric methods, ensuring secure access and seamless user experience."
version: 1.0.2
repository: https://github.com/P-YMR/flutter_auth_management.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  auth_management_delegates: ^1.0.4
  firebase_auth: ^5.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: